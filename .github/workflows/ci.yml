---
name: CI

on:
  pull_request:


concurrency:
  group: ${{ github.ref_name }}
  cancel-in-progress: true

jobs:
  puppet:
    name: Puppet
    uses: ekohl/gha-puppet/.github/workflows/beaker.yml@set-in-env-var
    with:
      pidfile_workaround: 'false'
      rubocop: false
      cache-version: '1'
